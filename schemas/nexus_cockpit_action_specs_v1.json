{
  "type": "object",
  "required": ["status", "bridge_version", "action_specs_version", "local_only", "actions"],
  "properties": {
    "status": {"const": "ok"},
    "bridge_version": {"type": "string"},
    "action_specs_version": {"const": "nexus_cockpit_action_specs_v1"},
    "local_only": {"type": "boolean", "const": true},
    "actions": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "action",
          "description",
          "required_fields",
          "optional_fields",
          "example_payload",
          "notes"
        ],
        "properties": {
          "action": {
            "type": "string",
            "enum": [
              "load_preset",
              "preview",
              "show_recent_artifacts",
              "generate_turn_packet"
            ]
          },
          "description": {"type": "string"},
          "required_fields": {"type": "array", "items": {"type": "string"}},
          "optional_fields": {"type": "array", "items": {"type": "string"}},
          "example_payload": {"type": "object"},
          "notes": {"type": "string"}
        },
        "additionalProperties": false
      }
    }
  },
  "additionalProperties": false
}
